# A14B Compatibility and Staged Transport — R2 + F01 Bounded Acceptance Record

Status: `RECORDED / ACCEPTED_WITHIN_SCOPE_CPU_AND_FIXTURE_LOOPBACK`

Date: `2026-09-12`

Task: `ACS-A14B-CONTRACT-COMPATIBILITY-AND-STAGED-TRANSPORT-R2-20260912`

Acceptance/publication task: `ACS-A14B-TRANSPORT-R2-F01-ACCEPTED-CODE-PUBLICATION-R1-20260912`

Base commit: `ad7349ff493baaa1e0bc831810ea28b3dd2b2dce`

Base tree: `a1f0f68ad69ea90d9c9cd96c2ed2ae33df4b2c93`

Branch: `feature/a14b-staged-transport-r2`

HISTORICAL_PATH_NOT_EXECUTION_AUTHORITY=true

## Authority and immutable history

The Project Lead explicitly authorized the [ADR-0022](../../governance/ADR-0022-generation-dispatch-grant.md)
v1.3 narrow compatibility increment and local staged-transport engineering. The R2
task package SHA-256 is `f534853983be877de027d959ed33ba96e7db915f7ea1a3f12f52a3b1e3370b6b`.
It does not change Grant/Terminal approval, revocation, CAS, coordination, lease,
slot, at-most-once or original Job/Attempt authority. Prior Package 1/2/3 acceptance
records and exact old three-model validation remain independent historical facts.

## Supplied Owner decision and exact accepted candidate

On `2026-09-12`, the Project Lead / Repository Governance Owner explicitly accepted
the exact candidate below for CPU/fixture-owned loopback engineering and confirmed
F01 closed for it. This records the supplied decision, not an executor-generated
signature or a fabricated approval time. The publication task authorizes five existing
document registrations, push, one PR, FULL_SUITE required checks, protected squash
merge and safe branch cleanup. It does not accept SH09 binding, real GPU or Spike-0.

| Identity | Value |
| --- | --- |
| Accepted R2 + F01 commit | `d0344a11709b8424a13b268545abfb8e580d17af` |
| Accepted tree | `a67a3e6a0db1e527de00ee86f7e78b74c1c9038d` |
| Parent R2 candidate | `84f76da87187f453e59afefa5d85b45250665fe8` |
| Parent R2 tree | `c6d253ca071fb58e768bbd18bbe7ecbaa6ded038` |
| Candidate archive SHA-256 | `db0b5617cf3f6b748393cd503ed37cd40bb3638a7bc4f99848c1b9cc9585eb55` |
| Candidate manifest SHA-256 | `4453d1af580b93ea5e12db804f578a2abbf84e24e6e23206404f4a76f6fe6c38` |
| Complete patch SHA-256 | `12753f790e873d620bc121a372a32dcb682e37bf73a44781142eda2784e32279` |

F01 adds the missing `flush()` delegation to the bounded response stream and three
focused regressions. The engineering set remains 31 paths and five new production
modules. Existing UNKNOWN, deadlines, L1/L2 and at-most-once semantics are unchanged.

## Bounded implementation

The new profile, compiler and runtime schemas distinguish six explicit model roles,
paired expert/LoRA stages, exact input contracts and a fixed 49-PNG output topology.
The engineering template is `TEST_ONLY`; it cannot establish SH09 exact binding.
Its backend class is restricted to fixture-owned loopback, not a live GPU endpoint.
The approved prompt is distinct from the original Script action and receives no
implicit camera suffix. Original upstream lineage is still read at L1 and L2.

The explicit transport factory has no import/constructor/open network effects.
The spent, in-process Capability authorizes one bounded write inside L2. Response,
history, downloads and postprocessing occur outside that gate. Network uncertainty
is preserved without retry, redirect, fallback or a second Attempt. A local sealed
submission identity is independent of the provider prompt identifier.

Native PNG content hashes and ordered indices are retained separately from the
derived MP4 hash. CPU postprocessing selects indices 0–47 and drops index 48, then
fresh ffprobe checks 704×1280, 48 frames, 24 fps and two seconds. The result boundary
uses the original artifact commit intent and durable replace. Unobserved provider
GPU usage and cost remain UNKNOWN, never Fake `false` or zero-cost evidence.

## Validation evidence and limits

The external candidate archive contains exact commands, failed and corrected raw
logs, scoped regression results, file hashes, the complete patch and a machine
manifest tied to its final local commit/tree. Tests use temporary SQLite databases,
synthetic media and HTTP servers created and held by the test fixtures. Existing
services, port 8188, SSH tunnels and formal data are not used. The local Linux CPU
runner exercises real fcntl/flock and FFmpeg; it is not an M12 build-host approval.
The original 311-test R2 regression belongs to parent tree
`c6d253ca071fb58e768bbd18bbe7ecbaa6ded038`, not the accepted F01 tree. The accepted
candidate has 38 distinct targeted tests: Unit 18, Contract 6 and Integration 14
(37 targeted plus one A14B vertical slice). The three after-fix tests are included
in those 38, not added again. Three independent socketpair component probes passed
separately; they are not a full project regression. Their before-image reproduced
the missing-flush errors. No local full Core or new 311-test run is claimed by this
registration. The final PR CI head, five required check results and merge identity
belong in the external publication receipt; no future merge SHA is predicted here.

One bounded search did not find the three required original frozen packages.
`A13=NOT_RUN_MISSING_ORIGINAL_EVIDENCE`; no synthetic skipped test substitutes for it.
The external intake record preserves the roots, names and results of that search.

```text
SH09_EXACT_BINDING=BLOCKED_MISSING_ORIGINAL_EVIDENCE
R2_ENGINEERING_OWNER_ACCEPTANCE=ACCEPTED_WITHIN_SCOPE_CPU_AND_FIXTURE_LOOPBACK
F01_OWNER_CLOSURE=CLOSED_FOR_EXACT_ACCEPTED_CANDIDATE
R2_ENGINEERING_CANDIDATE=d0344a11709b8424a13b268545abfb8e580d17af
PUBLICATION=NOT_YET_MERGED_AT_DOCUMENT_AUTHORING
REAL_COMFYUI_CONNECTIONS=0
REAL_PROMPT_SUBMISSIONS=0
GPU_EXECUTIONS=0
FORMAL_DATABASE_ACCESSES=0
NEW_PAID_OPERATIONS_STARTED=0
SYSTEM_RUNTIME_BOUND=false
PROMPT_SUBMISSION_AUTHORIZED=false
SPIKE_0_READINESS=BLOCKED
```

The finite engineering acceptance and code-publication authorization do not permit
real ComfyUI/GPU/SSH, formal databases, configuration deployment, formal Grant
issuance/consumption, real prompt submission, output admission, Master/Export or new
paid execution. Trusted original binding and every live operation require separate
evidence and authorization. Raw ZIPs, logs, approval bundles, configurations, host
directories and model files stay outside the public repository. No deployment or
runtime acceptance follows automatically from this record or its eventual merge.

# CCV-R1 Workflow Evidence Placeholder

> `EXPERIMENT EVIDENCE / NOT PRODUCTION CODE / NOT A MILESTONE DELIVERABLE`
>
> `SYNTHETIC_TEST_ONLY / NOT FOR PRODUCTION`

The three exact ComfyUI API workflows used on `2026-08-14` are not available in this
checkout. No guessed workflow JSON is supplied.

At the next authorized external-GPU evidence collection window, recover exactly:

```text
round-1.api.json
round-2.api.json
round-3.api.json
```

For each file, record byte size, SHA-256, ComfyUI commit, all custom-node commits and
the exact model records used. The evidence finalizer rejects a missing, zero-byte or
digest-mismatched workflow.

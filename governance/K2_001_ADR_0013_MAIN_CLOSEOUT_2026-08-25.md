# K2-001 / ADR-0013 Main Closeout — 2026-08-25

- Decision owner: Project Lead / Architecture Owner / Repository Governance Owner `蔺鹏`
- Decision: `OWNER ACCEPTED / COMPLETE / MAIN-VERIFIED`
- Scope: ADR-0013 non-GPU control-plane implementation only
- Core baseline: `6d28a53f3a077f032e341a87412b19b37c00bb1e`
- Core tree: `369c3b1479f3136cc32fcbc4efd0fa24e4964058`
- Frontend baseline: `5b36aac09fc10db04455d9ee287060232a521e5f`
- Frontend tree: `fd20b7d75c5ff379842462964d4e4f1d860d334d`
- Production Ready: `NO`
- Publication allowed: `false`

## 1. Owner acceptance

On 2026-08-25, the Project Lead accepted the ADR-0013 non-GPU control-plane
implementation as `OWNER ACCEPTED / COMPLETE / MAIN-VERIFIED` at the Core
baseline above.

Acceptance covers:

- the closed-world append-only evidence ledger;
- separate root, production, V4 runtime and semantic visual-QC projections;
- V5 as the sole `AssetVersion` authority;
- digest-pinned approval and selection authority;
- `Candidate → TechnicalValidation → SemanticVisualQCDecision →`
  `HumanSelectionDecision → AssetAdmission → AssetVersion`;
- V4 replay, recovery, no-clobber and orphan/quarantine controls;
- the sanitized Creator Public API projection and verified frontend consumers.

This decision supersedes only ADR-0013 statements that implementation acceptance
or merge to `main` remained pending. It does not convert technical execution into
media selection, admission, publication or Production Ready status.

## 2. Verification evidence

- Core local exact-baseline discovery: `851/851 PASS`, using
  `PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -p 'test*.py' -q`.
- Core final `main` Actions run `32802248611`: Unit 619, Integration 143,
  Contract 89, plus Markdown and documentation-link gates, all successful.
- Frontend local exact-baseline verification: Vitest `174/174 PASS`, typecheck,
  ESLint and production build successful.
- Frontend final `main` Actions run `32803363994`: `verify` and both Chromium
  gates successful.
- Core PR #10 and Frontend PRs #9/#10 have object histories consistent with
  rebase-and-merge.

## 3. K2-001 media truth

The implementation acceptance does not accept any K2-001 media.

- `rootState=ROOTS_READY`;
- `productionState=REAL_VIDEO_PLAN_READY`;
- four M11 v1 videos are technically verified;
- semantic visual QC is `FAIL`;
- all M11 v1 and Shot 01 R2–R7 candidates are unselected and not admitted;
- no video `AssetVersion`, `EpisodeMaster`, `ExportArtifact` or publication
  authorization exists;
- `publicationAllowed=false`.

Repository implementation and tests do not prove that the expected four
`Candidate + TechnicalValidation + SemanticVisualQCDecision(FAIL)` groups were
appended to the live canonical host database. No retrospective append or PASS is
claimed.

## 4. K2-001 disposition

`K2-001 = HISTORICAL VALIDATION EVIDENCE / CLOSED FOR FURTHER PRODUCTION`.

All records, receipts, digests, admitted M10 v1 image history, failed M11 evidence
and ADR decisions remain immutable audit history. No further generation,
selection, admission, Master, export or publication is authorized under K2-001.

The following tools and contracts remain K2-001-only historical operators and
must not be used to initialize K2-002:

- `scripts/k2_canonical_lineage_bootstrap.py`;
- `scripts/k2_canonical_lineage_api_verify.py`;
- `scripts/k2_m6_draft_operator.py`;
- `scripts/k2_preboot_validate.py`;
- `architecture/K2_CANONICAL_LINEAGE_BOOTSTRAP_CONTRACT.md`;
- `architecture/K2_INTERNAL_IMAGE_FIRST_REAL_MEDIA_REVISION_CONTRACT.md`.

ADR-0011's Rights/Provider/Budget exception remains bound to its exact historical
K2-001 internal run and is not inherited by K2-002.

## 5. Independently verified fact boundaries

### 5.1 Frontend Core pin

At the audit snapshot, Frontend `main@5b36aac0…` contained zero references to
`debc7fad` and `c56913c`. This is a `main`-only statement. Twenty non-`main`
remote branches still existed; `feature/k2-control-plane-remediation-ui` retained
four `debc7fad` references and two `c56913c` references. No all-remote-refs-zero
claim is made for that audit snapshot.

### 5.2 Frontend protection

At the audit snapshot, Frontend `main` reported `protected=false` and no active
repository ruleset. Branch protection was not enforced. A later configuration
change must have its own exported settings and verification evidence before the
status can be upgraded.

### 5.3 Core protection

At the audit snapshot, Core had an active ruleset, but it was not compliant with
the tracked specification: required approvals were `0`; merge, squash and rebase
were allowed; `Integration Tests` was not a required status check. `Active` did
not mean governance-compliant.

### 5.4 Actions and Cursor check suites

GitHub Actions completed successfully for both exact baselines. Each SHA also had
a separate empty Cursor check suite still `queued`, with no check runs and no
conclusion. The supported statement is “all GitHub Actions gates are green”, not
“all check suites completed”.

### 5.5 Core branch archive evidence

The 2026-08-25 archive-audit snapshot, captured before publication of the K2-002
working branch, independently verifies one Core branch and 51 self-consistent
annotated `archive/branches/*` tags whose peeled targets match their recorded source
tips. It does not independently reconstruct a historical starting count of 52
branches, prove every operation was non-force, prove temporary-auth cleanup or
validate the earlier `665.763s` local duration. The 9/42 split verifies tag-message
classification values; it is not a fresh semantic review of all 51 deleted branches.

### 5.6 Frontend initial pin-run fluctuation

The public initial pin-push log proves one failed UI test: `keeps the accepted Core
baseline usable when readiness is not advertised`, missing heading `可执行镜头图`.
Public evidence does not independently prove the numeric summary `173/174`. The
same SHA later passed its PR run; final `main` and the independent local rerun pass
`174/174`.

## 6. Transition

K2-002-CHANGAN is the active Internal Content Lab validation project. It must
create distinct Project, Series, Episode, production-run, authority, approval and
asset lineages in the existing canonical authority. It must not reuse K2-001 refs,
grants, evidence, operator manifests or package IDs.

The accepted transition authorizes non-GPU source-package review, canonical
registration-chain correction and EP01 preproduction only. It does not authorize
bulk generation, automatic semantic-QC PASS, automatic human selection, asset
admission, publication or reuse of the K2-001 internal execution exception.
